abstract class Durations {
  static const transition = Duration(milliseconds: 300);
  static const transitionToNavigate = Duration(seconds: 2);
  static const splashAnimation = Duration(seconds: 1);
  static const transitionDownload = Duration(seconds: 2);

  static const reportInterval = Duration(days: 29);
  static const lastReportInterval = Duration(days: 365);
}
